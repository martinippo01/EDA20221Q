import org.junit.jupiter.api.*;

public class SortedLinkedListTest {
/*
    @Test
    public void insertTest(){
        SortedListService<String> sortedList = new SortedLinkedList<String>();

        Assertions.assertTrue(sortedList.insert("Hola"));
        Assertions.assertTrue(sortedList.insert("Como"));
        Assertions.assertTrue(sortedList.insert("Estas"));
        Assertions.assertFalse(sortedList.insert("Hola"));

        boolean thr = false;
        try{
            sortedList.insert(null);
        }catch(IllegalArgumentException e){
            thr = true;
        }

        Assertions.assertTrue(thr);
    }
*/
}
